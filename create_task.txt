SQL>  CREATE TABLE TASK(TASK_ID NUMBER(3),
  2  TASK_NAME VARCHAR(100),
  3   PRJ_ID VARCHAR2(5), DNO VARCHAR2(3) ,FOREIGN KEY(PRJ_ID,DNO) REFERENCES PROJ(PRJ_NO,DNO)ON DELETE SET NULL,
  4  S_DATE DATE,
  5   E_DATE DATE,
  6  CHECK(E_DATE>S_DATE),
  7   STATUS VARCHAR(50));

Table created.

SQL> spool off;
