SQL> CREATE TABLE DEPT(DNO VARCHAR2(3) CONSTRAINT DNO_UNQ_CONS UNIQUE, CONSTRAINT D_LIKEDNO_CONS CHECK(DNO LIKE 'D%'),
  2   DNAME VARCHAR2(10) CONSTRAINT DNAME_UNQ_CONS UNIQUE,
  3   LOCATION VARCHAR2(9) CONSTRAINT VALID_LOC_CONS CHECK(LOCATION IN('BNG','MNG','MUB','HYD','CHN')));
CREATE TABLE DEPT(DNO VARCHAR2(3) CONSTRAINT DNO_UNQ_CONS UNIQUE, CONSTRAINT D_LIKEDNO_CONS CHECK(DNO LIKE 'D%'),
             *
ERROR at line 1:
ORA-00955: name is already used by an existing object 


SQL> drop table dept;
drop table dept
           *
ERROR at line 1:
ORA-02449: unique/primary keys in table referenced by foreign keys 


SQL> drop table emp;;
drop table emp;
              *
ERROR at line 1:
ORA-00933: SQL command not properly ended 


SQL> drop table emp;

Table dropped.

SQL> drop table proj;
drop table proj
           *
ERROR at line 1:
ORA-02449: unique/primary keys in table referenced by foreign keys 


SQL> drop table task;

Table dropped.

SQL> drop table proj;

Table dropped.

SQL> drop table dept;

Table dropped.

SQL> CREATE TABLE DEPT(DNO VARCHAR2(3) CONSTRAINT DNO_UNQ_CONS UNIQUE, CONSTRAINT D_LIKEDNO_CONS CHECK(DNO LIKE 'D%'),
  2   DNAME VARCHAR2(10) CONSTRAINT DNAME_UNQ_CONS UNIQUE,
  3   LOCATION VARCHAR2(9) CONSTRAINT VALID_LOC_CONS CHECK(LOCATION IN('BNG','MNG','MUB','HYD','CHN')));

Table created.

SQL> spool off;
